pipeline {
    agent {dockerfile true}
    stages {
        stage('DockerBuild') {
            steps ('Build file'){
                step(
                    [
                    $class: 'DockerBuilderPublisher',
                    cleanImages: false, 
                    cleanupWithJenkinsJobDelete: false,
                    //cloud: '',
                    dockerFileDirectory: 'static-site/', 
                    //fromRegistry: [], 
                    //pushCredentialsId: '',
                    pushOnSuccess: false,
                    tagsString: 'jenkinsdidit'
                    ]
                )
            }
        }
    }
}
